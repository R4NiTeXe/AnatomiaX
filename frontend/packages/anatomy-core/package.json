{
  "name": "@anatomiax/anatomy-core",
  "private": true,
  "version": "0.1.0",
  "description": "Platform-neutral anatomy logic for web and mobile - no React, no Three.js, no DOM",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "jest",
    "test:watch": "jest --watch",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "dependencies": {
    "@anatomiax/shared-types": "*"
  },
  "devDependencies": {
    "@types/jest": "^29.5.14",
    "jest": "^29.7.0",
    "prettier": "^3.3.3",
    "ts-jest": "^29.4.12",
    "typescript": "^5.5.4"
  }
}
