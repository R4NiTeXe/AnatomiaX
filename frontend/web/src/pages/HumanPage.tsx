import PagePlaceholder from '@/components/PagePlaceholder';

export default function HumanPage(): JSX.Element {
  return (
    <PagePlaceholder
      title="Human Anatomy Explorer"
      description="Interactive 3D human anatomy will open here. This route is a placeholder — the explorer ships in a later step."
    />
  );
}
