// Canonical system catalog lives in @anatomiax/anatomy-core (single source).
// This file re-exports for backward compatibility so existing imports keep working.
export * from '@anatomiax/anatomy-core';
