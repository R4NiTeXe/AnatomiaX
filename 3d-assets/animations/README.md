# Animations

Contains animation clips and related data for anatomy models.

Examples:

- `heart-beat.glb` (embedded animation)
- `breathing-cycle.glb`
- Separate clips referenced by systems if needed

Rules:

- Keep clips short and loopable where possible
- Prefer embedded GLB animations over separate files when practical
- Name with action: `verb-noun.glb`
- Optimize before committing

No animation assets are present yet.
