# Working — Male Whole-Body Split

This folder contains **generated working assets** split from the original source:

- Source: `3d-assets/male/source/hra-reference-organ-united-male-v1.5.glb` (146.48 MB, preserved unmodified, ignored via `.gitignore`)
- Generated files here are **not final production assets**
- They are for inspection and review of split quality, size, and separation
- The original source remains untouched at `3d-assets/male/source/`
- Final production assets will be reviewed, optimized (Draco/meshopt), and moved to `3d-assets/male/` (or `systems/`, `organs/` etc.) or CDN only after approval

Do not commit the `*.glb` files in this folder (ignored via `.gitignore`).
